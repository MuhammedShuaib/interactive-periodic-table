import React from "react";

const Header = () => (
  <header
    style={{
      textAlign: "center",
      padding: "1rem",
      fontSize: "2rem",
      fontWeight: "bold",
      color: "white",
    }}>
    Interactive Periodic Table
  </header>
);

export default Header;
